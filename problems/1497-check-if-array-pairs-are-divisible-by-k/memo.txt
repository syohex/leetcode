# memo

- arrのすべての余りを求める
  - C++は負数の数の余りを求めるとき負数が返るので, 負数が返ったときは正の値に変換する
  - 例 `-1 % 3 = -1` => `-1 + 3 => 2`
  - 余りを mapに入れ, 個数を管理する
- 0だけは特殊. `map[0]` が 2で割り切れるかを確認し, 割り切れなければ falseを返す
- 1 <= i <= k/2 でループ
  - `map[i]` と `map[k - i]` が同数かを確認する. 同数でなければ falseを返す
- ループを最後まで終えたら trueを返す 
